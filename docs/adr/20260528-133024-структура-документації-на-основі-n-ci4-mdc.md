---
session: ac62b1d9-3159-4158-abdd-721502f63409
captured: 2026-05-28T13:30:24+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-vitaliytv-myshare/ac62b1d9-3159-4158-abdd-721502f63409.jsonl
---

## ADR Структура документації на основі n-ci4.mdc

## Context and Problem Statement
Проєкт `myshare` мав єдиний `README.md` у корені репозиторія як джерело документації. Користувач поставив завдання створити повноцінний каркас документації згідно правила `.cursor/rules/n-ci4.mdc` і перенести туди вміст `README.md`, після чого видалити файл.

## Considered Options
* Повний каркас документації за arc42 + Diátaxis + MADR + C4 відповідно до `n-ci4.mdc`
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Повний каркас документації за arc42 + Diátaxis + MADR + C4 відповідно до `n-ci4.mdc`", because правило `n-ci4.mdc` є явно затвердженим стандартом документації у цьому репозиторії — користувач послався на нього як директиву.

### Consequences
* Good, because transcript фіксує очікувану користь: вміст `README.md` збережено у `docs/explanation/overview.md`, а кореневий файл видалено командою `rm README.md`.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
Створені файли та директорії:
- `docs/explanation/overview.md` — вміст колишнього `README.md`
- `docs/explanation/architecture.md` — arc42-скелет
- `docs/explanation/strategy.md` — Wardley Map (manual-зона)
- `docs/explanation/components/.gitkeep`
- `docs/how-to/run.md` — інструкція локального запуску
- `docs/reference/.gitkeep`
- `docs/snapshots/.gitkeep`
- `docs/adr/index.md` — індекс ADR з `AUTOGEN`-зоною
- `docs/glossary.md`
- `docs/.docgen/config.yaml`
- `docs/.docgen/manifest.json`
- `docs/.docgen/prompts/projection.md`

---

## ADR Видалення кореневого README.md

## Context and Problem Statement
Після перенесення вмісту `README.md` до `docs/explanation/overview.md` виникло питання, чи залишати `README.md` у корені репозиторія як точку входу для GitHub/GitLab або видалити його, щоб уникнути дублювання.

## Considered Options
* Видалити `README.md` повністю
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "Видалити `README.md` повністю", because користувач явно вказав "перенеси туди README.md а його видали" — видалення є частиною технічного завдання.

### Consequences
* Good, because transcript фіксує очікувану користь: єдине джерело істини — `docs/`, дублювання вмісту відсутнє.
* Bad, because Neutral, because transcript не містить підтвердження наслідку — відсутність `README.md` у корені може зламати стандартний рендер на GitHub/GitLab.

## More Information
Виконана команда: `rm /Users/vitalii/www/vitaliytv/myshare/README.md`
Вміст перенесено до: `docs/explanation/overview.md`

---

## ADR Конфігурація autogen-зон через docs/.docgen/

## Context and Problem Statement
Правило `n-ci4.mdc` передбачає гібридну модель документації: manual-зони та autogen-зони, що регенеруються з accepted ADR. Потрібно визначити де зберігати конфігурацію генератора та формат маніфесту.

## Considered Options
* Директорія `docs/.docgen/` з `config.yaml`, `manifest.json` і `prompts/`
* Інші варіанти в transcript не обговорювалися.

## Decision Outcome
Chosen option: "`docs/.docgen/` з `config.yaml`, `manifest.json` і `prompts/`", because такий підхід відповідає наявній інфраструктурі `scripts/docs-regen/` (файли `manifest.js`, `projection.js`, `templates.js`, `triggers.js`) та формату `n-ci4.mdc`.

### Consequences
* Good, because transcript фіксує очікувану користь: конфігурація ізольована у прихованій піддиректорії `docs/.docgen/`, не засмічує видиму документацію.
* Bad, because transcript не містить підтверджених негативних наслідків.

## More Information
- `docs/.docgen/config.yaml` — версія `1`, визначає `adr.dir`, `adr.status_marker` (`**Status:** Accepted`), `projections.prompts_dir`, `projections.manifest`
- `docs/.docgen/manifest.json` — початковий стан `{"zones": {}, "adr_index": {}}`
- `docs/.docgen/prompts/projection.md` — системний промпт для LLM-регенерації проекцій
- Наявний генератор: `scripts/docs-regen/` (`cli.js`, `manifest.js`, `projection.js`, `triggers.js`)
