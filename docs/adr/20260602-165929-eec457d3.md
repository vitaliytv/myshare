---
session: eec457d3-1708-4dbc-afa6-721d9bdf6138
captured: 2026-06-02T16:59:29+03:00
transcript: /Users/vitalii/.claude/projects/-Users-vitalii-www-vitaliytv-myshare/eec457d3-1708-4dbc-afa6-721d9bdf6138.jsonl
---

exit=0
[assistant]
Готово.

## Що зробив

**Твоє залізо:** M5, 16 GB RAM unified memory. Модель `gemma4:e4b` важить **8.9 GB** (8B параметрів, Q4_K_M) — майже половина всієї пам'яті системи. Коли Ollama тягне ваги + твоє оточення (Vite, Cargo, Zed, Codex, VS Code) — пам'яті не вистачає → macOS свопить → система стоїть. Це природне обмеження залізо/модель, не баг.

**Що виправив у коді:**

1. **`num_ctx: 8192`** у запиті до `/api/chat` — обмежує KV-кеш (контекст токенів). Без цього модель розміщує повний 131K-токенний буфер → в 10-15 разів більше пам'яті на генерацію.

2. **`unloadModel()` одразу після перекладу** — POST `/api/generate` з `keep_alive: 0` примушує Ollama вивантажити 8.9 GB ваг одразу, а не через 5 хвилин. Тепер після завершення перекладу система видихає.

**Що можна зробити на рівні залізо/налаштувань (поза кодом):**

- Поміняти модель на меншу: `qwen2.5:3b` або `phi4-mini` (~2-3 GB) дадуть швидший переклад без гальмування. Запуск: `ollama pull qwen2.5:3b` — і якщо захочеш, можу додати вибір моделі в UI.
- `OLLAMA_MAX_LOADED_MODELS=0` в `.zshrc` + рестарт `ollama serve` → модель не кешується взагалі.
- Закрити важкі застосунки на час перекладу (Codex/VS Code).
