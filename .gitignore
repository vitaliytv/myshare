# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets (e.g. Google OAuth client_secret). Public IDs live in app/src-tauri/.env
*.secret

# Generated by unplugin-auto-import
app/auto-imports.d.ts

# Claude Code agent worktrees (isolated working copies)
.claude/worktrees/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/hooks/.normalize-state

# Stryker mutation testing reports (summary lives in COVERAGE.md)
app/reports/
# cargo-mutants artifacts (mutants.out/ + mutants.out.old/ після повторних прогонів)
mutants.out*/

# docs:regen
docs/ci4/.regen.log
docs/ci4/.regen-debug/
.claude/hooks/.docs-regen.lock
# @nitra/cursor (adr) — локальні артефакти Stop-hook, не коміти
node_modules/
dist/
.claude/hooks/*.log
.claude/hooks/.normalize.lock

# coverage.js single-run lock
.coverage.lock

# Stryker mutation testing (test.mdc)
reports/stryker/incremental.json
**/reports/stryker/

# @nitra/cursor — локальні git-worktree, не коміти
.worktrees/

# Test artifacts: Stryker + coverage (test.mdc)
**/coverage/
